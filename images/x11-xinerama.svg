<svg width="700" height="300" xmlns="http://www.w3.org/2000/svg">
    <!-- Screen 0 (Virtual) -->
    <g>
        <!-- Screen 0 box -->
        <rect x="20" y="30" width="660" height="250" fill="#e3f2fd" stroke="#1976d2" stroke-width="3" rx="5"/>

        <!-- Screen 0 label -->
        <rect x="260" y="15" width="180" height="28" fill="#1976d2" rx="4"/>
        <text x="350" y="35" font-size="17" font-weight="bold" text-anchor="middle" fill="white">Screen 0 (3840x1080)</text>

        <!-- Root Window -->
        <rect x="35" y="55" width="630" height="210" fill="#bbdefb" stroke="#1976d2" stroke-width="2" rx="3"/>
        <text x="350" y="78" font-size="16" font-weight="bold" text-anchor="middle">Root Window</text>

        <!-- Divider line between monitors -->
        <line x1="350" y1="85" x2="350" y2="265" stroke="#1976d2" stroke-width="2" stroke-dasharray="5,5"/>

        <!-- Monitor 1 label -->
        <text x="190" y="100" font-size="14" text-anchor="middle" fill="#666">モニター1 (1920x1080)</text>

        <!-- Monitor 2 label -->
        <text x="510" y="100" font-size="14" text-anchor="middle" fill="#666">モニター2 (1920x1080)</text>

        <!-- Browser Window (Monitor 1, centered) -->
        <rect x="120" y="115" width="140" height="60" fill="#ffffff" stroke="#666" stroke-width="2" rx="3"/>
        <text x="190" y="135" font-size="32" text-anchor="middle">🌐</text>
        <text x="190" y="165" font-size="17" text-anchor="middle">ブラウザ</text>

        <!-- Terminal Window (Monitor 1, centered) -->
        <rect x="120" y="190" width="140" height="60" fill="#ffffff" stroke="#666" stroke-width="2" rx="3"/>
        <text x="190" y="210" font-size="32" text-anchor="middle">⌨️</text>
        <text x="190" y="240" font-size="17" text-anchor="middle">ターミナル</text>

        <!-- Text Editor Window (Monitor 2, centered) -->
        <rect x="440" y="152" width="140" height="60" fill="#ffffff" stroke="#666" stroke-width="2" rx="3"/>
        <text x="510" y="172" font-size="32" text-anchor="middle">📝</text>
        <text x="510" y="202" font-size="17" text-anchor="middle">テキストエディタ</text>
    </g>
</svg>
